import { BarChart3, CheckCircle2, Mail, Radar, TrendingUp } from "lucide-react";
import { prisma } from "@/lib/prisma";
import { getAccessContext } from "@/lib/role-access";

export default async function AnalyticsPage(){const{workspace,user,founder}=await getAccessContext();const id=workspace.id;const [leads,opportunities,tasks,activities]=await Promise.all([prisma.lead.findMany({where:{workspaceId:id,...(!founder?{assigneeId:user.id}:{})},select:{status:true,score:true,country:true,serviceCategory:true}}),prisma.opportunity.findMany({where:{workspaceId:id,...(!founder?{ownerId:user.id}:{})},include:{stage:true}}),prisma.task.findMany({where:{workspaceId:id,...(!founder?{assigneeId:user.id}:{})},select:{status:true,dueAt:true}}),prisma.activity.count({where:{workspaceId:id,...(!founder?{actorId:user.id}:{})}})]);const won=opportunities.filter(o=>o.stage.key==="won");const pipeline=opportunities.filter(o=>!o.stage.terminal).reduce((sum,o)=>sum+Number(o.valueMinor),0)/100;const cards=[[founder?"Leads collected":"My leads",leads.length,Radar],["High-score leads",leads.filter(l=>l.score>=80).length,TrendingUp],[founder?"Open pipeline":"My pipeline",`€${pipeline.toLocaleString("en-GB")}`,BarChart3],["Deals won",won.length,CheckCircle2],["Activities",activities,Mail],["Overdue tasks",tasks.filter(t=>t.status!=="COMPLETED"&&t.dueAt<new Date()).length,TrendingUp]] as const;const services=Object.entries(Object.groupBy(leads,l=>l.serviceCategory??"Other")).map(([name,items])=>({name,count:items?.length??0})).sort((a,b)=>b.count-a.count);return <><div className="list-heading"><div><p className="eyebrow">PERFORMANCE · {founder?"COMPANY":"MY RESULTS"}</p><h1>Analytics</h1><p>{founder?"Live operational metrics across Thrive Dev":"Your assigned leads, deals, activity and tasks"}</p></div></div><section className="analytics-cards">{cards.map(([label,value,Icon])=><article key={label}><Icon/><span>{label}</span><strong>{value}</strong></article>)}</section><div className="analytics-grid"><section><header><h2>Service demand</h2></header>{services.length?services.map(item=><div className="analytics-bar" key={item.name}><span>{item.name}</span><div><i style={{width:`${Math.max(8,item.count/Math.max(1,leads.length)*100)}%`}}/></div><strong>{item.count}</strong></div>):<p className="inline-empty">Lead data will appear here.</p>}</section><section><header><h2>Conversion snapshot</h2></header><dl><div><dt>Assigned leads</dt><dd>{leads.filter(l=>l.status==="ASSIGNED").length}</dd></div><div><dt>Qualified leads</dt><dd>{leads.filter(l=>l.status==="QUALIFIED").length}</dd></div><div><dt>Open deals</dt><dd>{opportunities.filter(o=>!o.stage.terminal).length}</dd></div><div><dt>Won value</dt><dd>{won.map(o=>`${o.currency} ${(Number(o.valueMinor)/100).toLocaleString("en-GB")}`).join(" · ")||"—"}</dd></div></dl></section></div></>}
